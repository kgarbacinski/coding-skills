import org.apache.commons.io.IOUtils;
import java.io.FileInputStream;

class TestChecker {
    public static void main(String[] args) {
       GetInputValues.fileGenerator();
    }

    public static void fileGenerator(){
        
    }

    public static String checker(){
        GetInputValues.checker()
    }

    public static String getInputValues(){
        GetInputValues.fileGenerator();
        String path = System.getProperty("user.dir") + 'exec_files/input_values.json';
        InputStream is = new FileInputStream(path);
        String jsonTxt = IOUtils.toString(is, "UTF-8");
        JSONObject json = new JSONObject(jsonTxt);
        return new Array(json.get('input'), json.get('output'));
    }
}
